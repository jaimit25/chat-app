apiVersion: v1
kind: Service
metadata:
  name: lb-nginx-service
  namespace: chat-app
spec:
  type: LoadBalancer
  selector:
    app: lb-nginx
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80